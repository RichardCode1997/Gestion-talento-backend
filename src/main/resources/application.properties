# mysql
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/bd_api_asistencias_sama?useSSL=false&serverTimeZone=UTC
spring.datasource.username=root
spring.datasource.password=root

# Configurar JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=true

# Perfil de desarrollo
spring.profiles.active=dev

# JWT Firma
#// IMPORTANTE: Para HS512, la firma debe tener al menos 64 caracteres.
#// Esta cadena tiene exactamente 64 caracteres para que no falle.
jwt.secret=EstaEsUnaFirmaMuySecretaYProtegidaParaGestionTalentoCibertec2026